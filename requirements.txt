# --- where to get the small CPU-only build of PyTorch ---
# (an index option must sit on its OWN line; it cannot follow a package name)
--extra-index-url https://download.pytorch.org/whl/cpu

# --- core: download, database, app ---
requests
numpy
pandas
duckdb
streamlit>=1.49
plotly

# --- weekly snapshot charts ---
matplotlib

# --- machine learning & graph analysis (link prediction) ---
torch
gensim
scikit-learn
