# Extra packages used only when checking the work:
#     pip install -r requirements-dev.txt
#
# mlxtend is a second, independent implementation of FP-Growth. test_fpgrowth.py
# compares our miner against it when it is installed and skips that comparison
# cleanly when it is not, so this file is optional. It is kept out of
# requirements.txt because nothing the app runs imports it, and a cloud
# deployment shouldn't install it just to run a test comparison.
# (scikit-learn is a separate, real dependency in requirements.txt --
# linkpred.py imports it directly for the link-prediction view.)
mlxtend>=0.23
